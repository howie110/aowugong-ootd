name: Android Release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Existing tag to release, for example v1.0.8"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-release:
    name: Build signed APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref }}

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Run tests
        run: flutter test

      - name: Generate Android signing files
        run: |
          mkdir -p android/keystore

          keytool -genkeypair \
            -v \
            -keystore android/keystore/release.jks \
            -storetype JKS \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -alias "aowugong" \
            -storepass "aowugong" \
            -keypass "aowugong" \
            -dname "CN=howie110, OU=aowugong-ootd, O=howie110, L=Unknown, ST=Unknown, C=CN"

          {
            echo "storeFile=keystore/release.jks"
            echo "storePassword=aowugong"
            echo "keyAlias=aowugong"
            echo "keyPassword=aowugong"
          } > android/key.properties

      - name: Build release APK
        run: |
          set -o pipefail
          if ! flutter build apk --release --no-tree-shake-icons 2>&1 | tee build-release.log; then
            {
              echo "### Build release APK failed"
              echo '```text'
              tail -n 120 build-release.log
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"

            message="$(tail -n 40 build-release.log | sed ':a;N;$!ba;s/%/%25/g;s/\r/%0D/g;s/\n/%0A/g')"
            echo "::error title=Build release APK failed::$message"
            exit 1
          fi

      - name: Prepare release asset
        id: asset
        env:
          TAG_NAME: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref_name }}
        run: |
          mkdir -p release
          VERSION="$(grep '^version:' pubspec.yaml | awk '{print $2}')"
          APK_NAME="daily-ootd-${TAG_NAME}-${VERSION}.apk"
          cp build/app/outputs/flutter-apk/app-release.apk "release/${APK_NAME}"
          echo "apk_path=release/${APK_NAME}" >> "$GITHUB_OUTPUT"

      - name: Upload APK to GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG_NAME: ${{ github.event_name == 'workflow_dispatch' && inputs.tag_name || github.ref_name }}
          APK_PATH: ${{ steps.asset.outputs.apk_path }}
        run: |
          if gh release view "$TAG_NAME" >/dev/null 2>&1; then
            gh release upload "$TAG_NAME" "$APK_PATH" --clobber
          else
            gh release create "$TAG_NAME" "$APK_PATH" --title "$TAG_NAME" --generate-notes
          fi
