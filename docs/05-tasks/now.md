# 当前任务清单

Status: active
Type: task-list
Last Updated: 2026-04-04
Source of Truth: yes
Related: [当前执行计划](../03-plans/current.md)

## Summary

本文件只记录当前最接近执行面的任务。完成或取消后需要及时更新，不把历史状态长期堆在这里。

## Phase 0: 文档确认

- [x] 确认主浏览形态使用固定比例网格
- [x] 确认照片只保存在 App 私有目录
- [x] 确认首页不保留日期范围筛选
- [x] 确认记录详情采用独立全屏
- [x] 确认创建 OOTD 时可从系统相册选择图片
- [x] 确认每条记录固定为 `1` 张主图和最多 `3` 张细节图
- [x] 确认删除标签只删除关联，重命名标签影响全部历史记录
- [x] 确认 MVP 移除气温和心情评分，仅保留备注
- [x] 确认 OOTD 具备“喜欢 / 不喜欢”状态，不喜欢进入可恢复垃圾箱
- [ ] 确认 `product-requirements.md`
- [ ] 确认 `mvp-scope.md`
- [ ] 确认 `data-model.md`
- [ ] 确认 `storage-strategy.md`
- [ ] 确认 `architecture-overview.md`
- [ ] 确认 ADR-001
- [ ] 确认 ADR-002

## Phase 1: 准备进入开发

- [ ] 创建 Flutter 项目骨架
- [ ] 建立 `lib/` 推荐目录结构
- [ ] 配置核心依赖
- [ ] 建立 Drift 数据库基础设施
- [ ] 打通最小闭环的技术实现清单

## Notes

- 文档未确认前，不进入正式代码实现。
- 当前已确认首版只保留首页和设置页。
- 当前已确认首页使用固定比例网格，详情使用独立全屏。
- 当前已确认每天只有一条 OOTD 记录，且固定为 `1` 张主图和最多 `3` 张细节图。
- 当前已确认创建 OOTD 时可从系统相册选择图片，但成品不进入系统相册。
- 当前已确认首页默认只展示“喜欢”的 OOTD，不喜欢的进入可恢复垃圾箱状态。
- 当前已确认设置页负责标签和“不喜欢” OOTD 的管理，首页负责按标签筛选 OOTD。
- 当前已确认删除标签只移除关联，重命名标签影响全部历史记录。
- 当前已确认 MVP 不做日期范围筛选。
- 当前已确认 MVP 移除气温和心情评分，仅保留备注。
- 当前已确认不做月历页、收藏和首次引导。
- 若任一关键假设变更，需要回写到 `01-product/` 或 `02-architecture/`。
